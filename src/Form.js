import React from 'react';

const styles = {
  main: {
    border: '1px #FD435B solid',
    height: '40em',
    width: '60em',
    marginTop: 10,
    marginBottom: 10,
  },
};

const Form = () => {
  return (
    <div style={styles.main}>
      <div>here is some stuff</div>
    </div>
  );
};

export default Form;
